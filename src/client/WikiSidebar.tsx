/**
 * 知识库边栏标签：vault 树 + 搜索 + lint 徽标（Task 3 完善）。
 * 当前为最小骨架，保证 client 构建通过。
 */
export function WikiSidebar() {
  return <div style={{ padding: 8, fontSize: 13, color: '#9ca3af' }}>知识库（v4 开发中）</div>
}
